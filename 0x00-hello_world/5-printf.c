#include <stdio.h>

/**
 * main - print the massage using char and printf
 *
 * Description: the function print the text using char and printf
 * Return: its's always 0
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");

	return (0);
}
