#include "main.h"

/**
 * main - entry point
 *
 * @argc: the numbers of args entered
 * @argv: the array of args availble
 * Return: it is 0
 */

int main(int argc, char* argv)
{
	_putchar("%d\n", argc);
	return (0);
}
