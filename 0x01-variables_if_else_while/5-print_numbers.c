#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - for loop inside
 *
 * Description: it print all the base of 10
 * Return: it is always 0
 */

int main(void)
{
	for (n = 0, n < 10, n++)
	{
		putchar(n);
	}
	putchar('\n');
	return (0);
}
